/*import React from "react";
import "../styles/Note.css"

function Note({ note, onDelete }) {
    const formattedDate = new Date(note.created_at).toLocaleDateString("en-US")

   /* return (
        <div className="layout">
          <Header />
          <SideBar />
          <main className="main-content">
            <div className="adicionar-tema">
              <h2>Adicione o Tema da Monografia</h2>
              <div className="views_info">
                {progress.length > 0 && (
                  <div className="SSEMessage">
                    {progress.map((msg, index) => (
                    
                    ))}
                  </div>
                )}
                {error && <div className="error-message">{error}</div>}
                {technicalError && (
                  <div className="error-message">Erro técnico: {technicalError}</div>
                )}
              </div>
              <form onSubmit={handleSubmit} className="form">
                <div className="form-group">
                  <input
                    type="text"
                    id="tema"
                    value={tema}
                    onChange={(e) => setTema(e.target.value)}
                    placeholder="Digite o tema da monografia"
                    required
                  />
                </div>
                <button type="submit">Enviar Tema</button>
              </form>
            </div>
             
           /* <ProgressComponent tema={tema} />
          </main>
        </div>
      );
}

export default Note*/
